projection;
strict ( 2 );

define behavior for zc_dpk_billingheader //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use association _Item { create; }
}

define behavior for zc_dpk_billingitem //alias <alias_name>
{
  use update;
  use delete;

  use association _Header;
}
