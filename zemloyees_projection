projection;
strict ( 2 );
use side effects;

use draft;

define behavior for zemloyees_projection //alias <alias_name>
{
  use create;
  use update;
  use delete;
  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;
}
