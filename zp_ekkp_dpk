projection;
strict ( 2 );

define behavior for zp_ekkp_dpk //alias <alias_name>
{
  use create;
  use update;
  use delete;

  use association _item { create; }
}

define behavior for zp_vbrp_dpk //alias <alias_name>
{
  use update;
  use delete;

  use association _Header;
}
