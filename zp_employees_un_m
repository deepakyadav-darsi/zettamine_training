projection;
strict ( 2 );
use draft;
extensible;

define behavior for zp_employees_un_m //alias <alias_name>
extensible
{
  use create;
  use update;
  use delete;

  use action SetActive;
  use action copy;
  use action upload;
  use action Resetall;

  use action Resume;
  use action Edit;
  use action Activate;
  use action Discard;
  use action Prepare;
}
